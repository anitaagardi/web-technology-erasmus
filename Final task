HTML:
- div, span, p, headings (h1-h6)
- images
- table
- links
- - form elements:
    	- text input: one line, multiple line (textarea)
    	- checkbox
    	- radio button
    	- color picker
    	- date picker
      - buttons
- ordered list
- unordered list
- it can multiple HTML files, link together
The HTML can have any topics.

CSS:
styling the HTML:
- table, form elements
- images (if needed)
- background
- text colors
